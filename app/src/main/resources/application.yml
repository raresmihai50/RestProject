spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/restproject_database
    username: restproject_user
    password: 1221
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      #ddl-auto: update
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect